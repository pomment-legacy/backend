#!/usr/bin/env node

const argv = require('minimist')(process.argv.slice(2));

if (argv.h || argv.help) {
    console.log('usage: pomment-config [data_directory]');
    process.exit(0);
}

require('../lib/config')(argv._[0]);
